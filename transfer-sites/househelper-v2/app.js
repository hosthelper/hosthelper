const cart={count:0};const toast=document.getElementById('toast');function showToast(msg){toast.textContent=msg;toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),1800)}function updateCart(){document.getElementById('cartCount').textContent=String(cart.count);document.getElementById('cartCountMobile').textContent=String(cart.count)}document.querySelectorAll('.add-cart').forEach(btn=>btn.addEventListener('click',()=>{cart.count+=1;updateCart();showToast(`${btn.dataset.name}을 장바구니에 담았습니다.`)}));document.querySelectorAll('.mini-product').forEach(btn=>btn.addEventListener('click',()=>{cart.count+=1;updateCart();showToast('반복구매 상품을 장바구니에 담았습니다.')}));document.querySelectorAll('[data-scroll]').forEach(btn=>btn.addEventListener('click',()=>document.querySelector(btn.dataset.scroll)?.scrollIntoView({behavior:'smooth'})));document.getElementById('findPackage').addEventListener('click',()=>{const guests=document.getElementById('guestCount').value;const budget=document.getElementById('budget').value;document.getElementById('packages').scrollIntoView({behavior:'smooth'});showToast(`${guests} · ${budget} 기준 추천 패키지를 보여드려요.`)});document.querySelectorAll('.category').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('.category').forEach(x=>x.classList.remove('active'));btn.classList.add('active');showToast(`${btn.querySelector('b').textContent} 상품을 준비 중입니다.`)}));document.getElementById('globalSearch').addEventListener('keydown',e=>{if(e.key==='Enter'){showToast(`“${e.target.value}” 검색 결과를 준비 중입니다.`)}});