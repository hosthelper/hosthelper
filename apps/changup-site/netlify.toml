# 창업정보 모임 정적 사이트 — Netlify 새 사이트 생성 시 Base directory를
# apps/changup-site 로 지정하면 이 파일이 자동 적용됩니다. 빌드 없음(순수 정적).
[build]
  base = "apps/changup-site"
  publish = "."
  command = "echo 'static site - no build needed'"
