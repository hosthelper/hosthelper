(()=>{
if((location.pathname.split('/').pop()||'').toLowerCase()!=='life.html')return;
const IMG=['https://images.unsplash.com/photo-1600607687920-4e2a09cf159d?auto=format&fit=crop&w=1200&q=84','https://images.unsplash.com/photo-1618773928121-c32242e63f39?auto=format&fit=crop&w=1200&q=84','https://images.unsplash.com/photo-1566665797739-1674de7a421a?auto=format&fit=crop&w=1200&q=84','https://images.unsplash.com/photo-1600566753190-17f0baa2a6c3?auto=format&fit=crop&w=1200&q=84'];
const stKo={requested:'요청 접수',proposed:'제안중',accepted:'제안 수락',confirmed:'예약 확정',checked_in:'체크인',checked_out:'체크아웃',settled:'정산 완료',cancelled:'취소',refunded:'환불 완료',sent:'제안 발송',declined:'거절',withdrawn:'철회',draft:'임시저장',published:'공개',reviewing:'검토중',pending:'대기',authorized:'승인',paid:'결제 완료',failed:'결제 실패',partial_refund:'부분 환불',requested_refund:'환불 요청',processed:'처리 완료',approved:'승인',rejected:'반려',ready:'정산 준비',held:'보류'};
const state={mode:'guest',page:'home',loading:true,spaces:[],requests:[],proposals:[],reservations:[],itinerary:[],notifications:[],conversations:[],messages:[],supplierServices:[],settlements:[],payments:[],refunds:[],roleRequests:[],selectedSpace:null,selectedRequest:null,selectedConversation:null,query:'',region:'전체',purpose:'전체'};
let root=null,modal=null,realtime=null;
const hu=()=>window.HU;
const esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
const won=v=>'₩'+Number(v||0).toLocaleString('ko-KR');
const date=v=>v?new Date(v).toLocaleDateString('ko-KR'):'-';
const dt=v=>v?new Date(v).toLocaleString('ko-KR',{month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit'}):'-';
const chip=(v,extra='')=>`<span class="lfChip ${extra}">${esc(stKo[v]||v||'-')}</span>`;
const img=(i=0)=>IMG[i%IMG.length];
function toast(msg,type='ok'){let e=document.getElementById('lfToast');if(!e){e=document.createElement('div');e.id='lfToast';e.style.cssText='position:fixed;right:18px;bottom:78px;z-index:500;background:#0d2b52;color:white;padding:11px 14px;border-radius:9px;font:11px Noto Sans KR;box-shadow:0 12px 35px #001b3c44;opacity:0;transition:.18s';document.body.appendChild(e)}e.textContent=msg;e.style.background=type==='bad'?'#a93f3f':'#0d2b52';e.style.opacity='1';setTimeout(()=>e.style.opacity='0',2600)}
function err(e,msg='처리 중 오류가 발생했습니다.'){console.error(e);toast(e?.message||msg,'bad')}
function user(){return hu()?.user||null}function uid(){return user()?.id||null}function isOp(){return !!hu()?.isOperator?.()}function has(r){return !!hu()?.hasRole?.(r)}
function canHost(){return has('host')||has('supplier')||isOp()}
function needLogin(){if(user())return true;document.querySelector('[data-hu-login]')?.click();return false}
function pages(){if(state.mode==='guest')return [['home','홈','⌂'],['search','숙소 검색','⌕'],['itinerary','나의 일정/예약','□'],['messages','메시지','◌'],['account','내 정보','○']];if(state.mode==='host')return [['host-dashboard','대시보드','⌂'],['host-requests','숙박 요청','▣'],['host-spaces','내 숙소 관리','⌂'],['host-reservations','예약 관리','□'],['host-calendar','일정 관리','◷'],['host-messages','메시지','◌'],['host-settlements','정산 관리','₩'],['host-supplier','공급 서비스','◇'],['host-account','내 정보','○']];return [['op-dashboard','대시보드','⌂'],['op-requests','요청 관리','▣'],['op-supply','숙소/공급자 관리','⌂'],['op-reservations','예약 관리','□'],['op-payments','결제/환불','₩'],['op-settlements','정산 관리','◎'],['op-users','회원/권한','○'],['op-messages','메시지','◌'],['op-analytics','통계 리포트','▥'],['op-audit','운영 로그','≡']]}
function pageAllowed(){if(state.mode==='host'&&!canHost()){state.mode='guest';state.page='home'}if(state.mode==='operator'&&!isOp()){state.mode='guest';state.page='home'}}
async function q(run){const r=await run();if(r?.error)throw r.error;return r?.data||[]}
async function loadData(){if(!hu()?.sb)return;state.loading=true;render();const sb=hu().sb;try{
 state.spaces=await q(()=>sb.from('hu_life_spaces').select('*').order('created_at',{ascending:false}).limit(100));
 if(!user()){state.requests=[];state.proposals=[];state.reservations=[];state.itinerary=[];state.notifications=[];state.conversations=[];state.messages=[];state.loading=false;render();return}
 const base=[q(()=>sb.from('hu_life_requests').select('*').order('created_at',{ascending:false}).limit(200)),q(()=>sb.from('hu_life_proposals').select('*').order('created_at',{ascending:false}).limit(200)),q(()=>sb.from('hu_life_reservations').select('*').order('created_at',{ascending:false}).limit(200)),q(()=>sb.from('hu_life_itinerary').select('*').order('start_at',{ascending:true}).limit(200)),q(()=>sb.from('hu_life_notifications').select('*').order('created_at',{ascending:false}).limit(100)),q(()=>sb.from('hu_conversations').select('*').eq('service','life').order('created_at',{ascending:false}).limit(100)),q(()=>sb.from('hu_life_supplier_services').select('*').order('created_at',{ascending:false}).limit(100))];
 [state.requests,state.proposals,state.reservations,state.itinerary,state.notifications,state.conversations,state.supplierServices]=await Promise.all(base);
 if(state.conversations.length){state.messages=await q(()=>sb.from('hu_messages').select('*').in('conversation_id',state.conversations.map(x=>x.id)).order('created_at',{ascending:true}).limit(500))}else state.messages=[];
 if(isOp()){[state.settlements,state.payments,state.refunds,state.roleRequests]=await Promise.all([q(()=>sb.from('hu_life_settlements').select('*').order('created_at',{ascending:false}).limit(200)),q(()=>sb.from('hu_life_payments').select('*').order('created_at',{ascending:false}).limit(200)),q(()=>sb.from('hu_life_refunds').select('*').order('requested_at',{ascending:false}).limit(200)),q(()=>sb.from('hu_role_requests').select('*').eq('service','life').order('created_at',{ascending:false}).limit(200))])}else{state.settlements=await q(()=>sb.from('hu_life_settlements').select('*').order('created_at',{ascending:false}).limit(100)).catch(()=>[]);state.payments=[];state.refunds=[];state.roleRequests=[]}
 }catch(e){err(e,'데이터를 불러오지 못했습니다.')}finally{state.loading=false;render()}}
function top(){const name=hu()?.profile?.display_name||user()?.email?.split('@')[0]||'회원';return `<header class="lfTop"><div class="lfBrand" data-nav="home"><span class="lfBrandMark">⌂</span><span>라이프헬퍼<small>LIFE HELPER</small></span></div><div class="lfModeTabs"><button class="lfMode ${state.mode==='guest'?'active':''}" data-mode="guest">회원</button>${user()?`<button class="lfMode ${state.mode==='host'?'active':''}" data-mode="host">호스트/공급자</button>${isOp()?`<button class="lfMode ${state.mode==='operator'?'active':''}" data-mode="operator">운영자</button>`:''}`:''}</div><div class="lfTopActions">${user()?`<span class="lfUser">${esc(name)} · ${esc(hu()?.profile?.helper_id||'Helper ID')}</span>${!canHost()?'<button class="lfBtn ghost" data-action="request-host-role">호스트/공급자 신청</button>':''}<button class="lfBtn ghost" data-hu-logout>로그아웃</button>`:'<span class="lfAuthHint">회원가입 후 예약·상담·일정을 관리할 수 있어요.</span><button class="lfBtn ghost" data-hu-signup>회원가입</button><button class="lfBtn" data-hu-login>로그인</button>'}</div></header>`}
function side(){return `<aside class="lfSide"><div class="lfSideTitle">${state.mode==='operator'?'OPERATOR CONTROL TOWER':state.mode==='host'?'HOST / SUPPLIER WORKSPACE':'MY LIFE HELPER'}</div>${pages().map(p=>`<button class="${state.page===p[0]?'active':''}" data-nav="${p[0]}"><b>${p[2]}</b><span>${p[1]}</span></button>`).join('')}</aside>`}
function mobileBottom(){const ps=state.mode==='guest'?[['home','홈','⌂'],['search','숙소','⌕'],['itinerary','일정','□'],['account','내 정보','○']]:pages().slice(0,4);return `<nav class="lfMobileBottom">${ps.map(p=>`<button class="${state.page===p[0]?'active':''}" data-nav="${p[0]}"><b>${p[2]}</b>${p[1]}</button>`).join('')}</nav>`}
function head(t,p='',actions=''){return `<div class="lfHead"><div><h1>${t}</h1><p>${p}</p></div>${actions?`<div class="lfHeadActions">${actions}</div>`:''}</div>`}
function stats(a){return `<div class="lfStats">${a.map(x=>`<div class="lfStat"><span>${x[0]}</span><b>${x[1]}</b><small>${x[2]||''}</small></div>`).join('')}</div>`}
function empty(t){return `<div class="lfEmpty">${esc(t)}</div>`}
function statusFlow(r){const all=['confirmed','checked_in','checked_out','settled'];const idx=all.indexOf(r.status);return `<div class="lfStatusFlow">${all.map((s,i)=>`<div class="lfFlowStep ${i<idx?'done':i===idx?'active':''}"><i>${i+1}</i><b>${stKo[s]}</b></div>`).join('')}</div>`}
function card(s,i){const d=s.details||{};return `<article class="lfCard" data-space="${s.id}"><img src="${esc(d.image_url||img(i))}"><div class="lfCardBody"><span class="lfChip good">검증된 숙소</span><h3>${esc(s.title)}</h3><p>${esc(s.region)} · 최대 ${s.capacity}명 · ${esc(d.type||'숙박시설')}</p><p>무료 Wi-Fi · ${esc(d.languages||'한국어, 영어 지원')}</p><div class="lfPrice">1박 ${won(s.nightly_price)}~</div></div></article>`}
function homePage(){const rec=state.spaces.filter(x=>x.status==='published').slice(0,3);return `<section class="lfPage active"><div class="lfHero"><div class="lfHeroCopy"><small>외국인 게스트도, 단체도, 행사 방문객도</small><h1>필요한 숙소를<br><em>더 빠르게 찾아드립니다.</em></h1><p>다양한 목적의 숙박을 한 번의 요청으로 가장 적합한 숙소와 연결해드립니다.</p><div class="lfHeroActions"><button class="lfBtn" data-action="open-request">숙박 요청하기 →</button><button class="lfBtn ghost" data-purpose="기업 행사">단체 문의하기</button></div></div><img src="${img(0)}"></div>${head('어떤 목적으로 한국에 오시나요?','목적을 선택하면 조건에 맞는 검증 숙소를 추천합니다.')}<div class="lfPurposeGrid">${[['🎪','행사 참가','전시회·컨퍼런스'],['🎓','학교·교육','어학연수·교환학생'],['🎵','공연·대회','콘서트·스포츠'],['💼','기업 행사','출장·워크숍'],['✚','의료','진료·검진'],['✈','일반 여행','관광·휴가']].map(x=>`<div class="lfPurpose" data-purpose="${x[1]}"><i>${x[0]}</i><b>${x[1]}</b><small>${x[2]}</small></div>`).join('')}</div><div style="margin-top:22px">${head('추천 숙소','검증 상태와 예약 가능성을 함께 확인하세요.','<button class="lfBtn ghost" data-nav="search">전체 보기 →</button>')}<div class="lfCards">${rec.length?rec.map(card).join(''):empty('공개된 숙소를 준비 중입니다.')}</div></div>`}
function searchPage(){let arr=state.spaces.filter(x=>x.status==='published');if(state.query)arr=arr.filter(x=>(x.title+' '+x.region+' '+JSON.stringify(x.details||{})).toLowerCase().includes(state.query.toLowerCase()));if(state.region!=='전체')arr=arr.filter(x=>x.region.includes(state.region));return `<section class="lfPage active">${head('숙소 검색','목적·지역·가격·숙소유형으로 검증 숙소를 찾습니다.','<button class="lfBtn" data-action="open-request">조건으로 요청하기</button>')}<div class="lfToolbar"><input id="lfSearch" value="${esc(state.query)}" placeholder="지역, 목적, 키워드로 검색"><select id="lfRegion"><option>전체</option><option ${state.region==='서울'?'selected':''}>서울</option><option ${state.region==='부산'?'selected':''}>부산</option><option ${state.region==='제주'?'selected':''}>제주</option></select><select><option>추천순</option><option>낮은 가격순</option><option>높은 가격순</option></select></div><div class="lfPanel"><div class="lfPanelTitle"><h2>총 ${arr.length}개의 숙소</h2><span>검증 공개 숙소</span></div><div class="lfCards">${arr.length?arr.map(card).join(''):empty('검색 조건에 맞는 숙소가 없습니다. 숙박 요청으로 맞춤 제안을 받아보세요.')}</div></div></section>`}
function detailPage(){const s=state.selectedSpace||state.spaces.find(x=>x.status==='published');if(!s)return searchPage();const d=s.details||{};return `<section class="lfPage active"><button class="lfBtn ghost" data-nav="search">← 목록으로</button><div class="lfGrid2" style="margin-top:10px"><section class="lfPanel"><img class="lfDetailHero" src="${esc(d.image_url||img(1))}"><div style="margin-top:12px"><span class="lfChip good">검증된 숙소</span><h1 style="font-size:24px;margin:8px 0">${esc(s.title)}</h1><p style="font-size:10px;color:#718499">★ ${esc(d.rating||'4.8')} · ${esc(s.region)} · 최대 ${s.capacity}명</p></div><div class="lfKV"><div><span>위치</span><b>${esc(s.region)}</b></div><div><span>체크인 / 체크아웃</span><b>${esc(d.checkin||'15:00')} / ${esc(d.checkout||'11:00')}</b></div><div><span>가격대</span><b>1박 ${won(s.nightly_price)}~</b></div><div><span>지원 언어</span><b>${esc(d.languages||'한국어 · 영어')}</b></div><div><span>숙소 유형</span><b>${esc(d.type||'호텔·레지던스')}</b></div><div><span>주요 시설</span><b>${esc(d.amenities||'Wi-Fi · 침구 · 냉난방')}</b></div></div><div class="lfHeroActions"><button class="lfBtn" data-action="contact-space" data-id="${s.id}">상담/문의하기</button><button class="lfBtn ghost" data-action="open-request" data-space-id="${s.id}">예약 요청하기</button></div></section><section class="lfPanel"><h2>숙소 안내</h2><p style="font-size:10px;line-height:1.8;color:#60758a">${esc(d.description||'실제 운영 여부와 기본 정보를 확인한 검증 숙소입니다. 예약 확정 전 호스트와 세부 조건을 상담할 수 있습니다.')}</p><div class="lfSuccess">✓ 라이프헬퍼 검증 숙소 · 실제 운영 여부와 기본 정보를 확인합니다.</div><h2 style="margin-top:16px">예약 흐름</h2><div class="lfTimeline"><article><b>숙박 요청</b><small>날짜·인원·목적·예산 입력</small></article><article><b>호스트 제안</b><small>숙소와 총 가격, 조건 제안</small></article><article><b>예약 확정</b><small>제안 수락 후 예약 생성</small></article><article><b>체크인 → 체크아웃 → 정산</b><small>전체 상태를 마이페이지에서 확인</small></article></div></section></div></section>`}
function itineraryPage(){if(!user())return loginGate('나의 일정/예약','로그인하면 예약과 여행 일정을 한 곳에서 관리할 수 있습니다.');const rs=state.reservations.filter(r=>r.user_id===uid());const items=state.itinerary.filter(i=>i.user_id===uid());return `<section class="lfPage active">${head('나의 여행 일정','숙소와 체험·식사·행사 일정을 한 화면에서 관리합니다.','<button class="lfBtn" data-action="add-itinerary">+ 일정 추가</button>')}<div class="lfGrid2"><section class="lfPanel"><h2>2026년 10월</h2><div class="lfCalendar">${['일','월','화','수','목','금','토',...Array.from({length:31},(_,i)=>i+1)].map(x=>`<span class="${x===22||x===23||x===24||x===25?'on':''}">${x}</span>`).join('')}</div><h2 style="margin-top:18px">내 일정</h2><div class="lfTimeline">${items.length?items.map(i=>`<article><b>${esc(i.title)}</b><small>${dt(i.start_at)} · ${esc(i.location||'')}</small></article>`).join(''):empty('아직 추가한 일정이 없습니다.')}</div></section><section class="lfPanel"><h2>예약 내역</h2>${rs.length?rs.map(r=>`<div class="lfPanel"><div class="lfPanelTitle"><h2>예약 ${r.id.slice(0,8)}</h2>${chip(r.status,r.status==='confirmed'?'good':'')}</div>${statusFlow(r)}<div class="lfKV"><div><span>총 예약금액</span><b>${won(r.total_amount)}</b></div><div><span>결제 상태</span><b>${esc(stKo[r.payment_status]||r.payment_status)}</b></div><div><span>환불 상태</span><b>${esc(stKo[r.refund_status]||r.refund_status)}</b></div><div><span>정산 상태</span><b>${esc(stKo[r.settlement_status]||r.settlement_status)}</b></div></div><div class="lfHeroActions">${r.status==='confirmed'?`<button class="lfBtn bad" data-action="cancel-reservation" data-id="${r.id}">예약 취소</button>`:''}${r.status==='cancelled'&&r.payment_status==='paid'?`<button class="lfBtn ghost" data-action="request-refund" data-id="${r.id}" data-amount="${r.total_amount}">환불 요청</button>`:''}${['checked_out','settled'].includes(r.status)?`<button class="lfBtn ghost" data-action="review" data-id="${r.id}">후기 작성</button>`:''}</div></div>`).join(''):empty('확정된 예약이 없습니다.')}</section></div></section>`}
function loginGate(t,p){return `<section class="lfPage active">${head(t,p)}<div class="lfPanel" style="max-width:620px;margin:50px auto;text-align:center;padding:34px"><h2>로그인이 필요합니다.</h2><p style="font-size:10px;color:#6d8196">회원가입 후 Helper ID로 요청·예약·채팅·일정이 연결됩니다.</p><button class="lfBtn" data-hu-login>로그인</button> <button class="lfBtn ghost" data-hu-signup>회원가입</button></div></section>`}
function accountPage(){if(!user())return loginGate('내 정보','Helper ID와 역할, 알림을 관리합니다.');const myReq=state.requests.filter(r=>r.user_id===uid());const myProps=state.proposals.filter(p=>myReq.some(r=>r.id===p.request_id));const notes=state.notifications.filter(n=>n.user_id===uid());return `<section class="lfPage active">${head('내 라이프헬퍼','요청→제안→예약→일정을 Helper ID로 연결합니다.')}<div class="lfBanner"><div><h2>${esc(hu()?.profile?.display_name||user().email)}</h2><p>Helper ID · ${esc(hu()?.profile?.helper_id||'-')}</p></div>${!canHost()?'<button class="lfBtn" data-action="request-host-role">호스트/공급자 신청</button>':''}</div>${stats([['내 숙박 요청',myReq.length+'건'],['받은 제안',myProps.length+'건'],['예약',state.reservations.filter(r=>r.user_id===uid()).length+'건'],['읽지 않은 알림',notes.filter(n=>!n.read_at).length+'건']])}<div class="lfGrid2"><section class="lfPanel"><h2>내 숙박 요청</h2>${myReq.length?`<div class="lfTableWrap"><table class="lfTable"><thead><tr><th>지역</th><th>기간</th><th>인원</th><th>상태</th><th>제안</th></tr></thead><tbody>${myReq.map(r=>`<tr><td>${esc(r.region)}</td><td>${date(r.start_date)}~${date(r.end_date)}</td><td>${r.guest_count}명</td><td>${chip(r.status)}</td><td><button class="lfMiniBtn primary" data-action="view-proposals" data-id="${r.id}">제안 보기</button></td></tr>`).join('')}</tbody></table></div>`:empty('숙박 요청을 만들어보세요.')}</section><section class="lfPanel"><div class="lfPanelTitle"><h2>알림</h2><button class="lfMiniBtn" data-action="read-all-notifications">모두 읽음</button></div>${notes.length?notes.slice(0,10).map(n=>`<div class="lfRequestItem"><b>${esc(n.title)}</b><small>${esc(n.body||'')} · ${dt(n.created_at)}</small>${!n.read_at?'<span class="lfChip warn">새 알림</span>':''}</div>`).join(''):empty('알림이 없습니다.')}</section></div></section>`}
function conversationPage(host=false){if(!user())return loginGate('메시지','로그인 후 호스트와 실시간 상담할 수 있습니다.');const cs=state.conversations;const selected=state.selectedConversation||cs[0];const ms=selected?state.messages.filter(m=>m.conversation_id===selected.id):[];return `<section class="lfPage active">${head(host?'게스트 메시지':'메시지','예약 전 상담과 예약 이후 문의를 이어서 관리합니다.')}<div class="lfPanel lfMessages"><div class="lfConversationList">${cs.length?cs.map(c=>`<div class="lfConversation ${selected?.id===c.id?'active':''}" data-conversation="${c.id}"><b>${esc(c.entity_type||'상담')} · ${c.entity_id?.slice(0,8)||''}</b><small>${dt(c.created_at)}</small></div>`).join(''):empty('아직 대화가 없습니다.')}</div><div class="lfChat">${selected?`<div class="lfChatBody">${ms.length?ms.map(m=>`<div class="lfBubble ${m.sender_id===uid()?'me':''}">${esc(m.body)}<small style="display:block;margin-top:3px;opacity:.65">${dt(m.created_at)}</small></div>`).join(''):empty('첫 메시지를 보내보세요.')}</div><form class="lfChatComposer" data-form="message"><input name="body" maxlength="4000" required placeholder="메시지를 입력하세요..."><input type="hidden" name="conversation" value="${selected.id}"><button class="lfBtn">전송</button></form>`:empty('왼쪽에서 대화를 선택하세요.')}</div></div></section>`}
function hostDash(){const rs=state.reservations.filter(r=>r.host_id===uid());const ps=state.proposals.filter(p=>p.host_id===uid());const spaces=state.spaces.filter(s=>s.owner_id===uid());return `<section class="lfPage active">${head('호스트/공급자 대시보드','오늘도 좋은 만남을 만들어주세요.')}<div class="lfBanner"><div><h2>${esc(hu()?.profile?.display_name||'호스트')}님,<br>오늘도 좋은 만남을 만들어주세요!</h2><p>숙박 요청부터 제안·예약·정산까지 한 곳에서 처리합니다.</p></div><button class="lfBtn" data-nav="host-requests">숙소 제안 보내기 →</button></div>${stats([['새로운 숙박 요청',state.requests.filter(r=>r.status==='requested').length+'건','실DB'],['진행 중 예약',rs.filter(r=>!['settled','cancelled','refunded'].includes(r.status)).length+'건','확정 포함'],['보낸 제안',ps.length+'건','호스트 기준'],['운영 숙소',spaces.length+'곳','내 숙소']])}<div class="lfGrid2"><section class="lfPanel"><h2>최근 숙박 요청</h2>${requestTable(state.requests.slice(0,8),true)}</section><section class="lfPanel"><h2>내 숙소 현황</h2>${spaces.length?spaces.slice(0,6).map((s,i)=>`<div class="lfRequestItem"><b>${esc(s.title)}</b><small>${esc(s.region)} · 최대 ${s.capacity}명 · ${won(s.nightly_price)}</small>${chip(s.status,s.status==='published'?'good':'warn')}</div>`).join(''):empty('내 숙소를 등록해주세요.')}</section></div></section>`}
function requestTable(arr,host=false){return arr.length?`<div class="lfTableWrap"><table class="lfTable"><thead><tr><th>고객/요청</th><th>여행기간</th><th>인원</th><th>지역</th><th>상태</th><th>관리</th></tr></thead><tbody>${arr.map(r=>`<tr><td>${esc(r.purpose||'숙박 요청')}<br><small>${r.id.slice(0,8)}</small></td><td>${date(r.start_date)}~${date(r.end_date)}</td><td>${r.guest_count}명</td><td>${esc(r.region)}</td><td>${chip(r.status,r.status==='requested'?'warn':'')}</td><td><div class="actions"><button class="lfMiniBtn primary" data-action="select-request" data-id="${r.id}">${host?'제안':'상세'}</button></div></td></tr>`).join('')}</tbody></table></div>`:empty('요청이 없습니다.')}
function hostRequests(){const r=state.selectedRequest||state.requests[0];const mine=state.spaces.filter(s=>s.owner_id===uid());return `<section class="lfPage active">${head('숙박 요청 상세 / 제안 관리','고객 조건을 확인하고 내 숙소와 가격을 제안합니다.')}<div class="lfSplit"><section class="lfPanel"><h2>숙박 요청 목록</h2><div class="lfRequestList">${state.requests.map(x=>`<div class="lfRequestItem ${r?.id===x.id?'active':''}" data-action="select-request" data-id="${x.id}"><b>${esc(x.purpose||'숙박 요청')} · ${esc(x.region)}</b><small>${date(x.start_date)}~${date(x.end_date)} · ${x.guest_count}명</small>${chip(x.status)}</div>`).join('')||empty('숙박 요청이 없습니다.')}</div></section><section class="lfPanel">${r?`<div class="lfPanelTitle"><h2>요청 상세</h2>${chip(r.status,r.status==='requested'?'warn':'')}</div><div class="lfKV"><div><span>여행 기간</span><b>${date(r.start_date)} ~ ${date(r.end_date)}</b></div><div><span>인원</span><b>${r.guest_count}명</b></div><div><span>여행 목적</span><b>${esc(r.purpose||'-')}</b></div><div><span>희망 지역</span><b>${esc(r.region)}</b></div><div><span>예산</span><b>${r.budget_min||r.budget_max?won(r.budget_min)+' ~ '+won(r.budget_max):'협의'}</b></div><div><span>요청사항</span><b>${esc(r.notes||'-')}</b></div></div><h2 style="margin-top:16px">숙소 제안하기</h2>${mine.length?`<form class="lfForm" data-form="proposal"><input type="hidden" name="request_id" value="${r.id}"><div class="lfField full"><label>내 숙소 선택</label><select name="space_id" required>${mine.map(s=>`<option value="${s.id}">${esc(s.title)} · 최대 ${s.capacity}명</option>`).join('')}</select></div><div class="lfField"><label>총 제안 가격</label><input type="number" name="price" min="0" required></div><div class="lfField"><label>가능 인원</label><input type="number" name="guests" min="1" value="${r.guest_count}" required></div><div class="lfField full"><label>지원 언어</label><input name="languages" value="한국어,영어"></div><div class="lfField full"><label>추가 메시지</label><textarea name="message" maxlength="1000"></textarea></div><div class="lfFormActions"><button class="lfBtn">제안 보내기</button></div></form>`:`<div class="lfNotice">제안하려면 먼저 내 숙소를 등록해야 합니다. <button class="lfMiniBtn" data-nav="host-spaces">숙소 등록</button></div>`}`:empty('요청을 선택하세요.')}</section></div></section>`}
function hostSpaces(){const mine=state.spaces.filter(s=>s.owner_id===uid());return `<section class="lfPage active">${head('내 숙소 관리','숙소를 등록하고 운영자가 검증한 뒤 공개합니다.','<button class="lfBtn" data-action="open-space-form">+ 숙소 등록</button>')}<div class="lfCards">${mine.length?mine.map(card).join(''):empty('등록한 숙소가 없습니다.')}</div></section>`}
function hostReservations(){const rs=state.reservations.filter(r=>r.host_id===uid());return `<section class="lfPage active">${head('예약 관리','예약 확정→체크인→체크아웃→정산 상태를 관리합니다.')}<div class="lfPanel">${rs.length?`<div class="lfTableWrap"><table class="lfTable"><thead><tr><th>예약</th><th>금액</th><th>결제</th><th>상태</th><th>다음 행동</th></tr></thead><tbody>${rs.map(r=>`<tr><td>${r.id.slice(0,8)}<br><small>${date(r.created_at)}</small></td><td>${won(r.total_amount)}</td><td>${chip(r.payment_status)}</td><td>${chip(r.status)}</td><td><div class="actions">${r.status==='confirmed'?`<button class="lfMiniBtn primary" data-action="transition" data-id="${r.id}" data-status="checked_in">체크인</button>`:''}${r.status==='checked_in'?`<button class="lfMiniBtn primary" data-action="transition" data-id="${r.id}" data-status="checked_out">체크아웃</button>`:''}${r.status==='checked_out'?`<button class="lfMiniBtn" data-action="transition" data-id="${r.id}" data-status="settled">정산완료</button>`:''}</div></td></tr>`).join('')}</tbody></table></div>`:empty('예약이 없습니다.')}</div></section>`}
function calendarPage(){const rs=state.reservations.filter(r=>r.host_id===uid());return `<section class="lfPage active">${head('일정 관리','체크인·체크아웃과 운영 일정을 확인합니다.')}<div class="lfGrid2"><section class="lfPanel"><h2>월간 일정</h2><div class="lfCalendar">${['일','월','화','수','목','금','토',...Array.from({length:31},(_,i)=>i+1)].map(x=>`<span class="${[22,23,24,25].includes(x)?'on':''}">${x}</span>`).join('')}</div></section><section class="lfPanel"><h2>예약 일정</h2><div class="lfTimeline">${rs.length?rs.map(r=>`<article><b>예약 ${r.id.slice(0,8)}</b><small>${chip(r.status)} · ${won(r.total_amount)}</small></article>`).join(''):empty('일정이 없습니다.')}</div></section></div></section>`}
function settlementPage(){const ss=state.settlements.filter(s=>s.host_id===uid());const rs=state.reservations.filter(r=>r.host_id===uid());return `<section class="lfPage active">${head('정산 관리','예약별 정산 예정·완료 상태를 확인합니다.')} ${stats([['예약 매출',won(rs.reduce((a,b)=>a+b.total_amount,0))],['정산 준비',ss.filter(s=>s.status==='ready').length+'건'],['정산 완료',ss.filter(s=>s.status==='paid').length+'건'],['보류',ss.filter(s=>s.status==='held').length+'건']])}<div class="lfPanel">${ss.length?`<div class="lfTableWrap"><table class="lfTable"><thead><tr><th>예약</th><th>총액</th><th>수수료</th><th>정산액</th><th>상태</th></tr></thead><tbody>${ss.map(s=>`<tr><td>${s.reservation_id.slice(0,8)}</td><td>${won(s.gross_amount)}</td><td>${won(s.fee_amount)}</td><td><b>${won(s.net_amount)}</b></td><td>${chip(s.status,s.status==='paid'?'good':'')}</td></tr>`).join('')}</tbody></table></div>`:empty('생성된 정산 내역이 없습니다.')}</div></section>`}
function supplierPage(){const xs=state.supplierServices.filter(x=>x.owner_id===uid());return `<section class="lfPage active">${head('공급 서비스 관리','숙박 외 체험·청소·교통·식음·B2B 서비스를 등록합니다.','<button class="lfBtn" data-action="open-supplier-form">+ 서비스 등록</button>')}<div class="lfCards">${xs.length?xs.map((x,i)=>`<article class="lfCard"><img src="${img(i+1)}"><div class="lfCardBody">${chip(x.status,x.status==='published'?'good':'warn')}<h3>${esc(x.title)}</h3><p>${esc(x.category)} · ${esc(x.region||'지역 협의')}</p><div class="lfPrice">${won(x.unit_price)}</div><p>${esc(x.description||'')}</p></div></article>`).join(''):empty('등록한 공급 서비스가 없습니다.')}</div></section>`}
function operatorDash(){return `<section class="lfPage active">${head('운영자 대시보드','숙박 요청→공급자 매칭→예약→결제/환불→정산을 한눈에 관리합니다.')} ${stats([['요청 접수',state.requests.filter(r=>r.status==='requested').length+'건'],['제안중',state.proposals.filter(p=>p.status==='sent').length+'건'],['예약 확정',state.reservations.filter(r=>r.status==='confirmed').length+'건'],['환불 요청',state.refunds.filter(r=>['requested','reviewing'].includes(r.status)).length+'건']])}<div class="lfGrid2"><section class="lfPanel"><h2>최근 숙박 요청</h2>${requestTable(state.requests.slice(0,8))}</section><section class="lfPanel"><h2>운영 상태</h2><div class="lfDonut"></div><div style="margin-top:14px">${['공개 숙소 '+state.spaces.filter(s=>s.status==='published').length+'곳','예약 '+state.reservations.length+'건','정산 '+state.settlements.length+'건'].map(x=>`<div class="lfRequestItem"><b>${x}</b></div>`).join('')}</div></section></div></section>`}
function opRequests(){return `<section class="lfPage active">${head('숙박 요청 매칭 관리','고객 요청을 확인하고 담당자·호스트/공급자 매칭을 관리합니다.')}<div class="lfPanel"><div class="lfToolbar"><input placeholder="고객, 지역, 요청번호 검색"><select><option>상태 전체</option><option>요청 접수</option><option>제안중</option><option>예약 확정</option></select></div>${requestTable(state.requests)}</div></section>`}
function opSupply(){return `<section class="lfPage active">${head('숙소 / 공급자 관리','등록된 숙소와 공급 서비스를 검증하고 공개 상태를 관리합니다.')}<div class="lfGrid2"><section class="lfPanel"><h2>숙소 검증</h2><div class="lfTableWrap"><table class="lfTable"><thead><tr><th>숙소</th><th>지역</th><th>상태</th><th>처리</th></tr></thead><tbody>${state.spaces.map(s=>`<tr><td>${esc(s.title)}</td><td>${esc(s.region)}</td><td>${chip(s.status)}</td><td><div class="actions"><button class="lfMiniBtn primary" data-action="space-status" data-id="${s.id}" data-status="published">공개</button><button class="lfMiniBtn" data-action="space-status" data-id="${s.id}" data-status="reviewing">검토</button></div></td></tr>`).join('')}</tbody></table></div></section><section class="lfPanel"><h2>공급 서비스</h2><div class="lfTableWrap"><table class="lfTable"><thead><tr><th>서비스</th><th>카테고리</th><th>상태</th><th>처리</th></tr></thead><tbody>${state.supplierServices.map(s=>`<tr><td>${esc(s.title)}</td><td>${esc(s.category)}</td><td>${chip(s.status)}</td><td><button class="lfMiniBtn primary" data-action="supplier-status" data-id="${s.id}" data-status="published">공개</button></td></tr>`).join('')}</tbody></table></div></section></div></section>`}
function opReservations(){return `<section class="lfPage active">${head('예약 · 일정 관리','예약 상태와 체크인/체크아웃을 운영자 권한으로 관리합니다.')}<div class="lfPanel"><div class="lfTableWrap"><table class="lfTable"><thead><tr><th>예약</th><th>회원</th><th>호스트</th><th>금액</th><th>결제</th><th>예약상태</th><th>처리</th></tr></thead><tbody>${state.reservations.map(r=>`<tr><td>${r.id.slice(0,8)}</td><td>${r.user_id.slice(0,8)}</td><td>${r.host_id.slice(0,8)}</td><td>${won(r.total_amount)}</td><td>${chip(r.payment_status)}</td><td>${chip(r.status)}</td><td><div class="actions">${r.status==='confirmed'?`<button class="lfMiniBtn" data-action="transition" data-id="${r.id}" data-status="checked_in">체크인</button>`:''}${r.status==='checked_in'?`<button class="lfMiniBtn" data-action="transition" data-id="${r.id}" data-status="checked_out">체크아웃</button>`:''}${r.status==='checked_out'?`<button class="lfMiniBtn primary" data-action="create-settlement" data-id="${r.id}">정산 준비</button>`:''}</div></td></tr>`).join('')}</tbody></table></div></div></section>`}
function opPayments(){return `<section class="lfPage active">${head('결제 / 환불 관리','결제대행사 연동 전에는 운영자가 실제 입금/결제 결과를 확인한 뒤 상태를 변경합니다.')}<div class="lfNotice">실제 카드 승인·취소는 PG 상점키가 연결되어야 합니다. 현재 DB/RLS/상태머신은 준비되어 있으며 허위 결제완료 처리를 자동 생성하지 않습니다.</div><div class="lfGrid2" style="margin-top:10px"><section class="lfPanel"><h2>예약 결제 상태</h2><div class="lfTableWrap"><table class="lfTable"><thead><tr><th>예약</th><th>금액</th><th>상태</th><th>처리</th></tr></thead><tbody>${state.reservations.map(r=>`<tr><td>${r.id.slice(0,8)}</td><td>${won(r.total_amount)}</td><td>${chip(r.payment_status)}</td><td><div class="actions"><button class="lfMiniBtn primary" data-action="mark-payment" data-id="${r.id}" data-status="paid">결제완료 확인</button><button class="lfMiniBtn bad" data-action="mark-payment" data-id="${r.id}" data-status="failed">실패</button></div></td></tr>`).join('')}</tbody></table></div></section><section class="lfPanel"><h2>환불 요청</h2><div class="lfTableWrap"><table class="lfTable"><thead><tr><th>예약</th><th>금액</th><th>사유</th><th>상태</th><th>처리</th></tr></thead><tbody>${state.refunds.map(r=>`<tr><td>${r.reservation_id.slice(0,8)}</td><td>${won(r.amount)}</td><td>${esc(r.reason||'-')}</td><td>${chip(r.status)}</td><td><div class="actions"><button class="lfMiniBtn primary" data-action="process-refund" data-id="${r.id}" data-approve="true">승인/처리</button><button class="lfMiniBtn bad" data-action="process-refund" data-id="${r.id}" data-approve="false">반려</button></div></td></tr>`).join('')}</tbody></table></div></section></div></section>`}
function opSettlements(){return `<section class="lfPage active">${head('정산 관리','호스트별 총액·수수료·정산액과 지급 상태를 관리합니다.')} ${stats([['정산 준비',state.settlements.filter(s=>s.status==='ready').length+'건'],['지급 완료',state.settlements.filter(s=>s.status==='paid').length+'건'],['보류',state.settlements.filter(s=>s.status==='held').length+'건'],['총 정산액',won(state.settlements.reduce((a,b)=>a+b.net_amount,0))])}<div class="lfPanel"><div class="lfTableWrap"><table class="lfTable"><thead><tr><th>예약</th><th>호스트</th><th>총액</th><th>수수료</th><th>정산액</th><th>상태</th><th>처리</th></tr></thead><tbody>${state.settlements.map(s=>`<tr><td>${s.reservation_id.slice(0,8)}</td><td>${s.host_id.slice(0,8)}</td><td>${won(s.gross_amount)}</td><td>${won(s.fee_amount)}</td><td><b>${won(s.net_amount)}</b></td><td>${chip(s.status)}</td><td><button class="lfMiniBtn primary" data-action="settlement-status" data-id="${s.id}" data-status="paid">지급완료</button></td></tr>`).join('')}</tbody></table></div></div></section>`}
function opUsers(){return `<section class="lfPage active">${head('회원 / 권한 승인','호스트·공급자 신청을 검토하고 승인 후 역할을 활성화합니다.')}<div class="lfPanel"><div class="lfTableWrap"><table class="lfTable"><thead><tr><th>신청자</th><th>역할</th><th>사유</th><th>상태</th><th>처리</th></tr></thead><tbody>${state.roleRequests.map(r=>`<tr><td>${r.user_id.slice(0,8)}</td><td>${esc(r.requested_role)}</td><td>${esc((r.note||'-').slice(0,45))}</td><td>${chip(r.status)}</td><td><div class="actions">${r.status==='pending'?`<button class="lfMiniBtn primary" data-action="role-review" data-id="${r.id}" data-status="approved">승인</button><button class="lfMiniBtn bad" data-action="role-review" data-id="${r.id}" data-status="rejected">반려</button>`:''}</div></td></tr>`).join('')}</tbody></table></div></div></section>`}
function opAnalytics(){const vals=[32,45,50,58,72,84];return `<section class="lfPage active">${head('통계 리포트','방문→요청→제안→예약→정산의 전환 흐름을 봅니다.')} ${stats([['전체 요청',state.requests.length+'건'],['제안',state.proposals.length+'건'],['예약',state.reservations.length+'건'],['예약 전환율',state.requests.length?Math.round(state.reservations.length/state.requests.length*100)+'%':'0%']])}<div class="lfGrid2"><section class="lfPanel"><h2>월별 전환 추이</h2><div class="lfChart">${vals.map(v=>`<i style="height:${v}%"></i>`).join('')}</div></section><section class="lfPanel"><h2>운영 게이트</h2><div class="lfTimeline"><article><b>Auth / Session</b><small>Supabase Auth 기반</small></article><article><b>RBAC / RLS</b><small>회원·호스트·공급자·운영자 분리</small></article><article><b>Lifecycle</b><small>요청→제안→예약→체크인/아웃→정산</small></article><article><b>Payment</b><small>PG 상점키 연결 필요</small></article></div></section></div></section>`}
function opAudit(){return `<section class="lfPage active">${head('운영 로그 / 시스템 상태','권한·예약·환불 상태 변경의 감사 로그를 확인합니다.')}<div class="lfPanel"><div class="lfNotice">브라우저에는 service-role 비밀키를 두지 않습니다. 모든 privileged action은 RLS/RPC를 통해 검증합니다.</div><h2 style="margin-top:14px">QA 체크</h2>${['모바일 회원 홈·검색·상세·일정','호스트 숙박요청·제안·예약·정산','운영자 매칭·결제·환불·권한승인','RLS 타인 데이터 차단','Loading / Empty / Error / Success 상태','API/DB CRUD 상태 반영'].map(x=>`<div class="lfRequestItem"><b>✓ ${x}</b></div>`).join('')}</div></section>`}
function page(){if(state.loading)return `<div class="lfLoading">라이프헬퍼 실데이터를 불러오는 중입니다...</div>`;const p=state.page;if(state.mode==='guest'){if(p==='home')return homePage();if(p==='search')return searchPage();if(p==='detail')return detailPage();if(p==='itinerary')return itineraryPage();if(p==='messages')return conversationPage();return accountPage()}if(state.mode==='host'){if(p==='host-dashboard')return hostDash();if(p==='host-requests')return hostRequests();if(p==='host-spaces')return hostSpaces();if(p==='host-reservations')return hostReservations();if(p==='host-calendar')return calendarPage();if(p==='host-messages')return conversationPage(true);if(p==='host-settlements')return settlementPage();if(p==='host-supplier')return supplierPage();return accountPage()}if(p==='op-dashboard')return operatorDash();if(p==='op-requests')return opRequests();if(p==='op-supply')return opSupply();if(p==='op-reservations')return opReservations();if(p==='op-payments')return opPayments();if(p==='op-settlements')return opSettlements();if(p==='op-users')return opUsers();if(p==='op-messages')return conversationPage(true);if(p==='op-analytics')return opAnalytics();return opAudit()}
function render(){if(!root)return;pageAllowed();root.innerHTML=top()+`<div class="lfLayout ${state.mode==='operator'?'lfOperatorDense':''}">${state.mode==='guest'?'':side()}<main class="lfMain">${page()}</main></div>`+mobileBottom()}
function openModal(html){modal.innerHTML=`<div class="lfModal"><button class="lfModalClose" data-action="close-modal">×</button>${html}</div>`;modal.classList.add('open')}
function closeModal(){modal.classList.remove('open')}
function requestForm(prefSpace=''){openModal(`${head('숙박 요청하기','조건을 입력하면 호스트/공급자가 맞춤 숙소와 가격을 제안합니다.')}<form class="lfForm" data-form="request"><div class="lfField"><label>희망 지역</label><input name="region" required placeholder="서울 마포구"></div><div class="lfField"><label>여행 목적</label><select name="purpose"><option>행사 참가</option><option>학교·교육</option><option>공연·대회</option><option>기업 행사</option><option>의료</option><option>일반 여행</option></select></div><div class="lfField"><label>체크인</label><input type="date" name="start" required></div><div class="lfField"><label>체크아웃</label><input type="date" name="end" required></div><div class="lfField"><label>인원</label><input type="number" name="guests" min="1" value="2" required></div><div class="lfField"><label>예산 최대</label><input type="number" name="budget_max" min="0" placeholder="예: 400000"></div><div class="lfField full"><label>요청사항</label><textarea name="notes" maxlength="1000" placeholder="유아용 침대, 늦은 체크인, 단체 이동 등"></textarea></div><input type="hidden" name="space_hint" value="${esc(prefSpace)}"><div class="lfFormActions"><button class="lfBtn">숙박 요청 보내기</button></div></form>`) }
function spaceForm(){openModal(`${head('내 숙소 등록','등록 후 운영자 검증을 거쳐 공개됩니다.')}<form class="lfForm" data-form="space"><div class="lfField full"><label>숙소명</label><input name="title" required></div><div class="lfField"><label>지역</label><input name="region" required></div><div class="lfField"><label>1박 가격</label><input type="number" name="price" min="0" required></div><div class="lfField"><label>최대 인원</label><input type="number" name="capacity" min="1" required></div><div class="lfField"><label>숙소 유형</label><input name="type" placeholder="호텔/레지던스/게스트하우스"></div><div class="lfField"><label>지원 언어</label><input name="languages" value="한국어,영어"></div><div class="lfField full"><label>소개</label><textarea name="description"></textarea></div><div class="lfField full"><label>대표 이미지 URL(선택)</label><input type="url" name="image_url"></div><div class="lfFormActions"><button class="lfBtn">검증 요청으로 등록</button></div></form>`) }
function supplierForm(){openModal(`${head('공급 서비스 등록','체험·청소·교통·식음·B2B 등 서비스를 등록합니다.')}<form class="lfForm" data-form="supplier"><div class="lfField"><label>카테고리</label><select name="category"><option>체험</option><option>청소</option><option>교통</option><option>식음</option><option>B2B</option><option>기타</option></select></div><div class="lfField"><label>서비스명</label><input name="title" required></div><div class="lfField"><label>단가</label><input type="number" name="price" min="0" required></div><div class="lfField"><label>지역</label><input name="region"></div><div class="lfField full"><label>설명</label><textarea name="description"></textarea></div><div class="lfFormActions"><button class="lfBtn">등록</button></div></form>`) }
function itineraryForm(){openModal(`${head('일정 추가','숙소 외 체험·식사·행사·메모를 여행 일정에 추가합니다.')}<form class="lfForm" data-form="itinerary"><div class="lfField"><label>유형</label><select name="type"><option value="experience">체험</option><option value="meal">식사</option><option value="event">행사</option><option value="transport">이동</option><option value="memo">메모</option></select></div><div class="lfField"><label>제목</label><input name="title" required></div><div class="lfField"><label>시작</label><input type="datetime-local" name="start" required></div><div class="lfField"><label>종료</label><input type="datetime-local" name="end"></div><div class="lfField full"><label>장소</label><input name="location"></div><div class="lfFormActions"><button class="lfBtn">일정 추가</button></div></form>`) }
async function act(a,el){const sb=hu().sb;try{if(a==='close-modal')return closeModal();if(a==='open-request'){if(!needLogin())return;return requestForm(el.dataset.spaceId||'')}if(a==='open-space-form')return spaceForm();if(a==='open-supplier-form')return supplierForm();if(a==='add-itinerary')return itineraryForm();if(a==='request-host-role'){if(!needLogin())return;const role=confirm('공급자 역할을 신청하려면 확인, 호스트 역할은 취소를 눌러주세요.')?'supplier':'host';const {error}=await sb.from('hu_role_requests').insert({user_id:uid(),service:'life',requested_role:role,note:'라이프헬퍼 '+role+' 역할 신청'});if(error)throw error;toast('역할 검증 요청을 보냈습니다.');return loadData()}if(a==='select-request'){state.selectedRequest=state.requests.find(r=>r.id===el.dataset.id);if(state.mode==='host')state.page='host-requests';render();return}if(a==='space-status'){const {error}=await sb.from('hu_life_spaces').update({status:el.dataset.status,updated_at:new Date().toISOString()}).eq('id',el.dataset.id);if(error)throw error;toast('숙소 상태가 변경되었습니다.');return loadData()}if(a==='supplier-status'){const {error}=await sb.from('hu_life_supplier_services').update({status:el.dataset.status,updated_at:new Date().toISOString()}).eq('id',el.dataset.id);if(error)throw error;toast('공급 서비스 상태가 변경되었습니다.');return loadData()}if(a==='transition'){const {error}=await sb.rpc('hu_life_transition_reservation',{p_reservation_id:el.dataset.id,p_new_status:el.dataset.status,p_reason:null});if(error)throw error;toast('예약 상태가 변경되었습니다.');return loadData()}if(a==='cancel-reservation'){const reason=prompt('취소 사유를 입력해주세요.');if(reason===null)return;const {error}=await sb.rpc('hu_life_transition_reservation',{p_reservation_id:el.dataset.id,p_new_status:'cancelled',p_reason:reason});if(error)throw error;toast('예약이 취소되었습니다.');return loadData()}if(a==='request-refund'){const reason=prompt('환불 요청 사유를 입력해주세요.');if(reason===null)return;const {error}=await sb.rpc('hu_life_request_refund',{p_reservation_id:el.dataset.id,p_amount:Number(el.dataset.amount),p_reason:reason});if(error)throw error;toast('환불 요청이 접수되었습니다.');return loadData()}if(a==='mark-payment'){if(!confirm('실제 결제/입금 확인 후에만 상태를 변경하세요. 계속할까요?'))return;const {error}=await sb.rpc('hu_life_operator_mark_payment',{p_reservation_id:el.dataset.id,p_status:el.dataset.status,p_provider:'manual',p_provider_ref:null});if(error)throw error;toast('결제 상태가 변경되었습니다.');return loadData()}if(a==='process-refund'){const {error}=await sb.rpc('hu_life_operator_process_refund',{p_refund_id:el.dataset.id,p_approve:el.dataset.approve==='true'});if(error)throw error;toast('환불 요청을 처리했습니다.');return loadData()}if(a==='role-review'){const {error}=await sb.from('hu_role_requests').update({status:el.dataset.status,reviewed_by:uid(),reviewed_at:new Date().toISOString()}).eq('id',el.dataset.id);if(error)throw error;toast('역할 신청을 처리했습니다.');return loadData()}if(a==='settlement-status'){const {error}=await sb.from('hu_life_settlements').update({status:el.dataset.status,paid_at:el.dataset.status==='paid'?new Date().toISOString():null,updated_at:new Date().toISOString()}).eq('id',el.dataset.id);if(error)throw error;toast('정산 상태가 변경되었습니다.');return loadData()}if(a==='create-settlement'){const r=state.reservations.find(x=>x.id===el.dataset.id);if(!r)return;const fee=Number(prompt('플랫폼 수수료 금액을 입력하세요. (정책 확정 전 자동 계산하지 않습니다.)','0'));if(!Number.isFinite(fee)||fee<0||fee>r.total_amount)return toast('올바른 수수료 금액을 입력해주세요.','bad');const {error}=await sb.from('hu_life_settlements').upsert({reservation_id:r.id,host_id:r.host_id,gross_amount:r.total_amount,fee_amount:fee,net_amount:r.total_amount-fee,status:'ready',updated_at:new Date().toISOString()},{onConflict:'reservation_id'});if(error)throw error;await sb.from('hu_life_reservations').update({settlement_status:'ready'}).eq('id',r.id);toast('정산 준비 내역을 생성했습니다.');return loadData()}if(a==='contact-space'){if(!needLogin())return;const s=state.spaces.find(x=>x.id===el.dataset.id);if(!s?.owner_id)return toast('호스트가 연결되지 않은 숙소입니다.','bad');let c=state.conversations.find(x=>x.entity_type==='space'&&x.entity_id===s.id&&((x.created_by===uid()&&x.counterpart_id===s.owner_id)||(x.created_by===s.owner_id&&x.counterpart_id===uid())));if(!c){const {data,error}=await sb.from('hu_conversations').insert({service:'life',created_by:uid(),counterpart_id:s.owner_id,entity_type:'space',entity_id:s.id}).select().single();if(error)throw error;c=data}state.selectedConversation=c;state.page='messages';toast('호스트와 상담방을 열었습니다.');await loadData();return}if(a==='read-all-notifications'){const {error}=await sb.from('hu_life_notifications').update({read_at:new Date().toISOString()}).eq('user_id',uid()).is('read_at',null);if(error)throw error;toast('알림을 모두 읽음 처리했습니다.');return loadData()}if(a==='view-proposals'){const req=state.requests.find(r=>r.id===el.dataset.id);const ps=state.proposals.filter(p=>p.request_id===el.dataset.id);openModal(`${head('받은 숙소 제안',req?esc(req.region)+' · '+date(req.start_date)+'~'+date(req.end_date):'')} ${ps.length?ps.map(p=>`<div class="lfPanel"><div class="lfPanelTitle"><h2>제안 ${p.id.slice(0,8)}</h2>${chip(p.status)}</div><div class="lfKV"><div><span>총 가격</span><b>${won(p.total_price)}</b></div><div><span>가능 인원</span><b>${p.max_guests||'-'}명</b></div></div><p style="font-size:10px;color:#667b90">${esc(p.message||'')}</p>${p.status==='sent'?`<button class="lfBtn" data-action="accept-proposal" data-id="${p.id}">이 제안 수락하고 예약하기</button>`:''}</div>`).join(''):empty('아직 받은 제안이 없습니다.')}`);return}if(a==='accept-proposal'){const {error}=await sb.rpc('hu_accept_life_proposal',{p_proposal_id:el.dataset.id});if(error)throw error;closeModal();toast('제안을 수락해 예약을 생성했습니다.');state.page='itinerary';return loadData()}if(a==='review'){const rating=Number(prompt('평점을 1~5로 입력해주세요.','5'));if(!(rating>=1&&rating<=5))return;const comment=prompt('후기를 입력해주세요.')||'';const r=state.reservations.find(x=>x.id===el.dataset.id);const p=state.proposals.find(x=>x.id===r?.proposal_id);const {error}=await sb.from('hu_life_reviews').insert({reservation_id:r.id,space_id:p?.space_id||null,user_id:uid(),rating,comment});if(error)throw error;toast('후기를 등록했습니다.');return loadData()}}catch(e){err(e)}}
async function submit(form){const sb=hu().sb,fd=new FormData(form),kind=form.dataset.form;try{if(kind==='request'){const start=fd.get('start'),end=fd.get('end');if(new Date(end)<=new Date(start))return toast('체크아웃은 체크인 이후여야 합니다.','bad');const {error}=await sb.from('hu_life_requests').insert({user_id:uid(),region:fd.get('region'),start_date:start,end_date:end,guest_count:Number(fd.get('guests')),purpose:fd.get('purpose'),budget_max:fd.get('budget_max')?Number(fd.get('budget_max')):null,notes:(fd.get('notes')||'')+(fd.get('space_hint')?`\n희망 숙소:${fd.get('space_hint')}`:'')});if(error)throw error;closeModal();toast('숙박 요청이 접수되었습니다.');state.page='account';return loadData()}if(kind==='space'){const {error}=await sb.from('hu_life_spaces').insert({owner_id:uid(),title:fd.get('title'),region:fd.get('region'),nightly_price:Number(fd.get('price')),capacity:Number(fd.get('capacity')),status:'reviewing',details:{type:fd.get('type'),languages:fd.get('languages'),description:fd.get('description'),image_url:fd.get('image_url')}});if(error)throw error;closeModal();toast('숙소를 검증 요청으로 등록했습니다.');return loadData()}if(kind==='supplier'){const {error}=await sb.from('hu_life_supplier_services').insert({owner_id:uid(),category:fd.get('category'),title:fd.get('title'),description:fd.get('description'),unit_price:Number(fd.get('price')),region:fd.get('region'),status:'reviewing'});if(error)throw error;closeModal();toast('공급 서비스를 등록했습니다.');return loadData()}if(kind==='proposal'){const langs=String(fd.get('languages')||'').split(',').map(x=>x.trim()).filter(Boolean);const {error}=await sb.from('hu_life_proposals').insert({request_id:fd.get('request_id'),host_id:uid(),space_id:fd.get('space_id'),total_price:Number(fd.get('price')),max_guests:Number(fd.get('guests')),languages:langs,message:fd.get('message'),status:'sent'});if(error)throw error;toast('고객에게 제안을 보냈습니다.');return loadData()}if(kind==='itinerary'){const {error}=await sb.from('hu_life_itinerary').insert({user_id:uid(),item_type:fd.get('type'),title:fd.get('title'),start_at:new Date(fd.get('start')).toISOString(),end_at:fd.get('end')?new Date(fd.get('end')).toISOString():null,location:fd.get('location')});if(error)throw error;closeModal();toast('일정에 추가했습니다.');return loadData()}if(kind==='message'){const body=String(fd.get('body')||'').trim();if(!body)return;const {error}=await sb.from('hu_messages').insert({conversation_id:fd.get('conversation'),sender_id:uid(),body});if(error)throw error;form.reset();return loadData()}}catch(e){err(e)}}
function bind(){document.addEventListener('click',async ev=>{if(!root&&!modal)return;const nav=ev.target.closest('[data-nav]');if(nav&&root?.contains(nav)){state.page=nav.dataset.nav;render();window.scrollTo({top:0,behavior:'smooth'});return}const mode=ev.target.closest('[data-mode]');if(mode&&root?.contains(mode)){const m=mode.dataset.mode;if(m==='host'&&!canHost()){toast('승인된 호스트/공급자 권한이 필요합니다.','bad');return}if(m==='operator'&&!isOp()){toast('운영자 권한이 필요합니다.','bad');return}state.mode=m;state.page=m==='guest'?'home':m==='host'?'host-dashboard':'op-dashboard';render();return}const s=ev.target.closest('[data-space]');if(s&&root?.contains(s)){state.selectedSpace=state.spaces.find(x=>x.id===s.dataset.space);state.page='detail';render();window.scrollTo({top:0,behavior:'smooth'});return}const purpose=ev.target.closest('[data-purpose]');if(purpose&&root?.contains(purpose)){state.purpose=purpose.dataset.purpose;state.page='search';state.query=state.purpose;render();return}const conv=ev.target.closest('[data-conversation]');if(conv&&root?.contains(conv)){state.selectedConversation=state.conversations.find(x=>x.id===conv.dataset.conversation);render();return}const a=ev.target.closest('[data-action]');if(a&&(root?.contains(a)||modal?.contains(a)))return act(a.dataset.action,a)});document.addEventListener('submit',ev=>{const f=ev.target.closest('[data-form]');if(!f)return;ev.preventDefault();submit(f)});document.addEventListener('input',ev=>{if(ev.target.id==='lfSearch'){state.query=ev.target.value;clearTimeout(window.__lfSearchT);window.__lfSearchT=setTimeout(render,180)}});document.addEventListener('change',ev=>{if(ev.target.id==='lfRegion'){state.region=ev.target.value;render()}})}
async function init(){if(!window.HU?.sb){setTimeout(init,120);return}document.body.classList.add('lifeV5Ready');root=document.createElement('div');root.id='lifeV5';modal=document.createElement('div');modal.className='lfModalBack';document.body.prepend(root);document.body.appendChild(modal);bind();hu().sb.auth.onAuthStateChange(()=>setTimeout(loadData,200));try{realtime=hu().sb.channel('lifehelper-v5-live').on('postgres_changes',{event:'*',schema:'public',table:'hu_messages'},()=>loadData()).on('postgres_changes',{event:'*',schema:'public',table:'hu_life_requests'},()=>loadData()).on('postgres_changes',{event:'*',schema:'public',table:'hu_life_reservations'},()=>loadData()).subscribe()}catch(e){console.warn('realtime unavailable',e)}await loadData()}
init();
})();