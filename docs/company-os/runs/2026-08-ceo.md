# 대표실 실행 기록 — 2026년 8월

> 작성일 **2026-08-07**. `.claude/agents/hu-ceo.md`의 출력 계약을 따라 작성했습니다.
> **여기 나오는 목표 숫자는 전부 제안이며 대표 확정 전입니다.** 확정되지 않은 숫자는 `제안 — 확정 필요`로 표시했습니다.
> 근거는 모두 저장소에서 직접 확인한 것입니다. 실측 성과 숫자(도달·리드 건수)는 확인할 방법이 없어 `unknowns`에 있습니다.

---

## goal_sentence

> 이번 달은 새 콘텐츠를 만들지 않고, **이미 만들어 둔 자산이 채널에 실제로 나가고 그 유입이 측정되는 상태**를 만든다.

---

## weakest_link

| 필드 | 값 |
|---|---|
| `stage` | **인지** |
| `brand` | 공실헬퍼 (창업정보 모임 계열, `@changupmoim`) |
| `symptom` | 사람이 들어오지 않는다 |
| `why_weakest` | 대표 지목과 일치. 다만 **원인이 콘텐츠 부족이 아니다** — 아래 검증 참조 |

### 대표 직감 검증 — 원인은 "제작"이 아니라 "발행·측정"

약한 고리가 인지라는 판단은 맞습니다. 그런데 흔한 대응인 "콘텐츠를 더 만들자"는 **이 상황에서 틀립니다.**

| 확인한 것 | 사실 | 출처 |
|---|---|---|
| 자산은 충분 | 카드뉴스 **154장**(26 prefix), 릴스 완성본 **11편**, 라이브 기사 **17편**. 채널별 훅 뱅크·요일 운영표·양도인 스크립트 4종까지 완비 | `apps/changup-site/mkt/cardnews/`, `reel/reel_*.mp4`, `app.html` ARTICLES, `mkt/*.md` |
| 발행이 멈춤 | 기사 최종 발행 **2026-07-29** — 오늘 기준 **9일 공백**. 자동 워크플로는 스케줄이 제거돼 수동 전용·폐기 예정 | `app.html`의 `var TODAY='2026.07.29'`, `.github/workflows/changup-content.yml` |
| 게시가 안 나감 | `publish-reel.yml`은 `reel_*.mp4` push로 트리거되는데, 앞단 합성 워크플로 4개가 커밋 메시지에 **`[skip ci]`** 를 붙여 발화하지 않음 | `.github/workflows/build-reel-daily.yml:77` 외 3건, `mkt/CHANNEL_EXPANSION.md`가 결함으로 명시 |
| 측정이 안 됨 | 유입 태깅 로직은 utm → sessionStorage → referrer → direct 3단 폴백으로 **100% 동작**하는데, 실제 **태깅 실적 0건** | `apps/changup-site/s/index.html:201-219`, `mkt/CHANNEL_OPS_PLAYBOOK.md`·`SELLER_OUTBOUND.md` |

**결론**: 릴스 11편과 카드뉴스 154장을 만들어 놓고 게시 파이프라인이 끊긴 채 두 달 가까이 흘렀습니다. 여기서 콘텐츠를 더 만들면 안 나가는 자산만 늘어납니다.

원문 PDF의 자동화 원칙 — *"먼저 수동으로 한 번 성공 → 반복 구간 확인 → 그 부분만 자동화 → 실패 알림"* — 에서 지금은 **첫 단계인 "한 번 성공"조차 끊겨 있는 상태**입니다.

---

## must_do

| # | title | why |
|---|---|---|
| 1 | 게시 파이프라인 복구 | 릴스 11편이 저장소에 있는데 채널로 나가지 않는다. 이걸 고치면 추가 제작 없이 노출이 생긴다 |
| 2 | 유입 태깅 실측 시작 | 태깅 0건이면 어느 채널이 되는지 모른다. 측정 없이는 다음 달 판단도 직감이 된다 |
| 3 | 기사 발행 재개 | 9일 공백. 사이트에 신선도가 없으면 유입돼도 신뢰를 잃는다 |

## stop_doing

| # | title | why_not_now |
|---|---|---|
| 1 | 새 카드뉴스·릴스 제작 | 154장·11편이 안 나가고 있다. 재고가 쌓인 상태에서 생산을 늘릴 이유가 없다 |
| 2 | 신규 채널 개설 | 기존 채널 성과조차 측정 못 하는 상태다. 채널을 늘리면 측정 부채만 커진다 |
| 3 | 하우스헬퍼·헬퍼아카데미 확장 | 생애주기 앞단(인지)이 막혀 있다. 뒷단 브랜드를 밀어도 들어올 사람이 없다 |

---

## priorities

| rank | title | owner_dept | funnel_stage |
|---|---|---|---|
| 1 | `publish-reel` 발화 단절 진단과 수정 (`[skip ci]` 문제) | 개발 | 인지 |
| 2 | 게시 파이프라인 전체 분해 + 실패 알림 설계 | 운영 | 인지 |
| 3 | 기사 발행 재개 — 기존 카테고리 4종 내에서 주제 3개 | 마케팅 | 인지 |
| 4 | 미게시 자산(릴스 11편·카드뉴스 26 prefix) 재활용 게시 순서 확정 | 마케팅 | 인지 |
| 5 | 모든 게시 링크에 utm 강제 — 태깅 0건 탈출 | 운영 | 가입 |

**부서 3개**(개발·운영·마케팅)만 가동합니다. 영업·고객지원·재무는 이번 달 배정하지 않습니다 — 인지가 뚫리기 전에는 뒷단에 넘길 물량이 없습니다.

---

## assignments

### 개발팀 — `hu-build`

| 필드 | 값 |
|---|---|
| `first_task` | `build-reel-daily.yml`의 합성 커밋이 `[skip ci]`를 붙여 `publish-reel.yml`이 발화하지 않는 문제를 고친다. `[skip ci]` 제거 또는 `publish-reel`을 `workflow_run` 트리거로 전환 중 무한루프 위험이 낮은 쪽을 고른다 |
| `done_when` | 릴스 1편이 **사람 개입 없이** 게시 워크플로까지 도달한 것을 Actions 실행 로그로 확인. 로그 링크를 기록에 남긴다 |
| `input_needed` | 없음 — 저장소 안에서 확인 가능 |
| 주의 | 결과 커밋이 자기 자신을 다시 트리거하지 않도록 `paths`·브랜치 조건을 유지할 것. 고친 뒤 **실제 1회 실행으로 검증**하고, 안 돌면 "고쳤다"고 하지 말 것 |

### 운영팀 — `hu-ops`

| 필드 | 값 |
|---|---|
| `first_task` | 릴스 제작→게시 흐름을 시작조건·입력·처리·결과·실패알림으로 분해하고, 사람이 유지할 구간(최종 발행 승인)과 기계 구간을 나눈다. 실패 알림 문구를 만든다 |
| `done_when` | 실패했을 때 **누구에게 무엇이 통보되는지** 한 줄로 적혔고, 킬 스위치 조건이 정해졌다 |
| `input_needed` | 개발팀의 파이프라인 수정 결과 |
| 주의 | 알림 채널은 `publish-reel.yml`이 이미 쓰는 텔레그램(`TELEGRAM_BOT_TOKEN`·`TELEGRAM_CHAT_ID`) 재사용 가능. 최종 발행을 자동 승인으로 만들지 말 것 |

### 마케팅팀 — `hu-marketing`

| 필드 | 값 |
|---|---|
| `first_task` | ① 기사 주제 3개(기존 카테고리 4종 내) ② 미게시 릴스 11편·카드뉴스 26 prefix의 재활용 게시 순서 |
| `done_when` | 오늘 바로 올릴 수 있는 주제 3개가 근거와 함께 나왔고, 재활용 순서에 각 자산의 `prefix`가 명시됐다 |
| `input_needed` | 없음 — 훅 뱅크와 자산 목록이 저장소에 있다 |
| 주의 | **새 훅을 짜기 전에 `mkt/CHANNEL_OPS_PLAYBOOK.md` 훅 뱅크를 먼저 확인.** 채널 계열(`@changupmoim` / `helperuniverse01`)을 섞지 말 것 — CTA 키워드와 utm 규약이 다르다 |

---

## start_today

| 필드 | 값 |
|---|---|
| `title` | `publish-reel` 발화 단절 진단 |
| `dept` | 개발 |
| `input_ready` | **있음** — 워크플로 파일 5개가 저장소에 있고 결함 위치가 특정돼 있다 |
| `missing_input` | 없음 |

인지 회복의 나머지가 전부 이 하나에 걸려 있습니다. 게시가 안 나가는 상태에서는 주제를 몇 개 뽑든 도달이 0입니다.

---

## 30일 북극성 — 제안

| 항목 | 값 |
|---|---|
| 원래 지표 | 유료 결제 건수 |
| **이번 달 제안** | **설문 리드 제출 건수** (선행지표 "가입")로 판정 — `제안 — 확정 필요` |
| 제안 이유 | 결제까지 리드타임이 길고, 지금은 인지가 끊긴 상태라 결제로 재면 이번 달 개선이 신호로 잡히지 않습니다. 파이프라인이 복구되면 다음 달 결제로 되돌립니다 |
| 참고선 | `mkt/CHANNEL_OPS_PLAYBOOK.md`의 기존 목표 "첫 4주 안에 설문 리드 누적 20건 + 양도 매물 3건". **달성 여부는 확인 불가** |
| 숫자 | `___건` ← 대표가 채웁니다 |

북극성 자체(유료 결제)는 바꾸지 않습니다. 이번 달 **판정 기준**만 선행지표로 당기자는 제안입니다.

---

## unknowns

판단에 구멍이 있는 항목입니다. 이 숫자들을 모으는 일이 우선순위보다 먼저일 수 있습니다.

| 항목 | 왜 필요한가 | 누가 줄 수 있나 |
|---|---|---|
| 4채널 실제 도달·조회 | 인지가 얼마나 막혔는지 정량 확인 | mirra 대시보드 (`uni` 스킬 §8) |
| 설문 리드 누적 건수와 상태 분포 | 이번 달 목표 숫자를 정하려면 기준선이 필요 | Supabase 대시보드 (`/ops`는 이 리드를 보지 못함) |
| 채널별 유입 성과 | 어디에 집중할지 판단 | 태깅 0건 — **지금은 알 수 없음** |
| "4주 20건 + 매물 3건" 달성 여부 | 기존 목표가 유효한지 | 대표 확인 |
| 이번 달 북극성 숫자 | 달성/미달 판정 기준 | 대표 확정 |
| 이번 달 하지 않을 일 확정 | 위 `stop_doing` 3건 승인 여부 | 대표 확정 |

---

## 다음 실행

1. 개발팀 → 운영팀 → 마케팅팀 순으로 부릅니다. `assignments`를 **요약하지 말고 원문 그대로** 넘깁니다.
2. 월말에 재무팀(`hu-finance`)으로 유지/중단/확대를 판단하고 그 결과를 다음 달 대표실 입력으로 넣습니다.
3. 인수인계 규칙은 [`docs/AI_COMPANY_OS.md` §4](../../AI_COMPANY_OS.md#4-인수인계-흐름).
